# 统一以 LF 存入仓库，检出时按平台还原。
# 避免 Windows 开发时整文件被标记为改动，产生无意义的 diff 噪音。
* text=auto eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary

# 锁文件与生成物在 diff 中折叠显示
pnpm-lock.yaml linguist-generated=true -diff
